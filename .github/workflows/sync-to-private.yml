name: Sync Changes to Private Repo

on:
  push:
    branches:
      - main

jobs:
  sync-to-private:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout public repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout private repo
        uses: actions/checkout@v4
        with:
          repository: hankhank10/lawmaker
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}
          path: private-repo
          fetch-depth: 0

      - name: Create branch and sync changes
        env:
          PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          cd private-repo

          # Configure git
          git config user.name "lawmaker-wiki-bot"
          git config user.email "action@github.com"

          # Create a new branch for the sync
          BRANCH_NAME="wiki-sync-$(date +%Y%m%d-%H%M%S)"
          git checkout -b "$BRANCH_NAME"

          # Copy all files from public repo to game-manual folder
          rm -rf game-manual/*
          cp -r ../docs ../mkdocs.yml ../requirements.txt ../README.md game-manual/ 2>/dev/null || true
          cp -r ../.gitignore game-manual/ 2>/dev/null || true

          # Check if there are any changes
          if git diff --quiet; then
            echo "No changes to sync"
            exit 0
          fi

          # Commit changes
          git add game-manual/
          git commit -m "📝 Sync documentation changes from public wiki

Changes synced from https://github.com/hankhank10/lawmaker-wiki

Co-Authored-By: lawmaker-wiki contributors"

          # Push branch
          git push origin "$BRANCH_NAME"

          # Create PR using GitHub CLI
          gh pr create \
            --repo hankhank10/lawmaker \
            --base main \
            --head "$BRANCH_NAME" \
            --title "📝 Update game manual from public wiki" \
            --body "## Summary
This PR syncs documentation changes from the public wiki repository.

### Source
- Repository: https://github.com/hankhank10/lawmaker-wiki
- Branch: main

### Changes
Public contributors have made improvements to the documentation. Please review and merge if the changes look good.

🤖 Auto-generated by wiki sync workflow"
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
